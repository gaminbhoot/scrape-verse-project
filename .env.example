BRIGHT_DATA_API_KEY=your_bright_data_token_here
# Set to false to force real CLI even without key (will error clearly)
ALLOW_MOCK_FALLBACK=true
DATABASE_URL=file:./data/aegis.db

# Set to true to force REST POST /dca/trigger instead of CLI (requires live collector)
BRIGHT_DATA_USE_REST=false
